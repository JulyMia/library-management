# 实验一与实验二完成及报告撰写指南

## 1. 先说结论

基于我对 `SE6317 软件体系结构-实验报告模板v4(2).docx`、`SE6317_软件体系结构_实验指导书_v4(2).pdf` 和 `README.md` 的阅读，针对你当前这个图书馆管理系统，最稳妥、最容易落地、也最容易写出前后对比结果的方案是：

- 实验一选题：以**可读性与可修改性**为主题，重点写“AI 生成代码可运行但不易维护”的问题分析与重构过程。
- 实验二选题：以**可测试性**为主题，重点写“当前业务逻辑与 I/O、持久化耦合，导致难测试”，再通过解耦和补充测试完成改进。
- 报告组织方式：**将实验一和实验二合并为一份实验报告提交**，这是指导书明确允许的。
- 实验二不建议优先选“可调试性”，因为当前项目是命令行本地程序，没有复杂日志链路、远程调试、分布式追踪场景；相比之下，“可测试性”更容易做出明确的结构改进、测试代码和覆盖率数据。

## 2. 指导书与模板中的关键要求

从指导书和模板中，可以提炼出以下硬性或强相关要求：

- 实验一与实验二的报告**可以合并提交**。
- 实验报告必须包含：需求描述、系统结构、重构或改进过程、度量结果、代码示例、结论。
- 必须附上 **Git 提交历史截图**，建议至少展示 3 次关键提交。
- 如果使用了 AI 辅助编码、报告润色、思路启发，必须在报告中加入 **AI 使用声明**。
- 实验一要求关注可读性/可修改性，并结合静态分析、复杂度、可维护性等度量结果。
- 实验二要求关注可调试性或可测试性，至少选一个深入展开，并结合测试、覆盖率、解耦、Mock 等内容。
- 两个实验都强调：不能只说“改了什么”，还要说明“为什么改”“指标怎么变了”“AI 代码有哪些典型问题”。

## 3. 当前项目为什么适合这样做

你这个项目本身就是一个典型的实验对象，`README.md` 已经明确说明它刻意保留了一些“AI 初始生成代码常见问题”，非常适合实验一和实验二：

- `main.py` 中多个菜单函数偏长，输入采集、流程控制、输出展示混在一起。
- `services.py` 中业务方法同时承担查找、校验、状态修改和持久化，职责过重。
- 多处存在重复校验逻辑，例如“查找用户/图书/记录”“重复借阅检查”“库存检查”等。
- 存在 `data`、`temp`、`flag`、`result`、`info` 等泛化命名。
- 当前项目没有测试代码，天然适合作为实验二中“从低可测试性到可测试性提升”的案例。
- `LibraryService` 虽然允许注入 `storage`，但整体仍然把数据加载、业务处理、保存动作耦合在一起，测试粒度不够细。

## 4. 可直接写进实验一的问题点

实验一建议你聚焦“可修改性”，同时兼顾可读性。这样更容易和后续重构动作形成对应关系。

### 4.1 可以在报告里点名的问题

1. 菜单层与业务层边界不够清晰
   `main.py` 中 `book_menu()`、`user_menu()`、`borrow_menu()` 都比较长，输入解析和业务调用交织在一起，后续增加新功能时修改范围大。

2. 服务层函数职责过重
   `services.py` 中 `borrow_book()`、`return_book()`、`update_book()`、`update_user()` 同时处理对象查找、参数校验、业务规则、状态变更和持久化保存，不符合单一职责原则。

3. 重复代码较多
   `services.py` 中大量 for 循环用于查找图书、用户、记录对象；重复借阅、库存不足、用户状态异常等校验散落在业务方法中，后续规则变化时需要修改多处。

4. 命名可读性一般
   出现 `temp`、`data`、`flag`、`result` 这类宽泛变量名，不利于快速理解代码意图。

5. 规则与数据持久化耦合
   许多方法在业务刚完成时立刻调用 `save_books()`、`save_users()`、`save_records()`，导致后续如果调整保存策略或引入事务式保存，会牵动多处修改。

6. 配置缺少持久化
   默认借阅天数只保存在 `LibraryService.default_borrow_days` 中，程序重启后丢失，规则配置与系统数据没有统一管理。

7. 异常与边界处理较弱
   当前主要以 `(bool, str)` 返回结果文本为主，适合初学者理解，但不利于后续扩展出更清晰的异常类别，也不利于针对异常路径做测试。

### 4.2 实验一最推荐的改进动作

建议你从下面 5 类改动中选 3 到 5 类去做，工作量适中，报告也好写：

1. 提取公共查找方法
   将图书、用户、借阅记录查找和“不存在时的响应”做统一封装，减少重复逻辑。

2. 拆分长业务函数
   例如把 `borrow_book()` 拆成：
   - 查找并校验用户
   - 查找并校验图书
   - 校验借阅资格
   - 创建借阅记录
   - 保存数据

3. 统一命名
   把 `temp`、`data`、`flag`、`result` 这类变量换成更明确的业务语义名，如 `book_to_delete`、`target_user`、`isbn_exists`、`matched_records` 等。

4. 抽取校验函数
   把 ISBN 唯一性、手机号唯一性、重复借阅、借阅数量上限等规则提取成私有方法。

5. 改善菜单层结构
   将 `main.py` 中每个菜单选项对应的处理逻辑再拆成更小的方法，例如 `_handle_add_book()`、`_handle_update_user()` 等。

### 4.3 实验一可以使用的度量

推荐至少做下面几种：

- `Pylint`：展示命名、函数复杂度、代码风格、重复逻辑倾向等问题。
- `Radon`：
  - 圈复杂度 `CC`
  - 可维护性指数 `MI`
  - 原始代码统计 `raw`
- `GitHub CodeQL`：指导书明确要求必须使用，至少在仓库中接入并展示扫描结果截图。

你目录里已经有 `pylint-before.txt`、`radon-cc-before.txt`、`radon-mi-before.txt`、`radon-raw-before.txt`，这非常适合当作“重构前基线数据”。你后续只需要在重构后重新执行同类工具，形成 before/after 对比表。

## 5. 可直接写进实验二的问题点

实验二建议你选“可测试性”，不要分散去写“可调试性 + 可测试性”两个主题，否则篇幅会被摊薄。

### 5.1 当前项目的可测试性短板

1. 没有任何测试代码
   当前仓库中没有 `pytest`/`unittest` 测试文件，这一点本身就可以作为实验二的出发点。

2. 业务规则集中但耦合度偏高
   虽然规则大多集中在 `LibraryService`，便于找到测试对象，但方法内部同时操作内存数据和持久化保存，导致单元测试不够纯粹。

3. 时间相关逻辑缺少显式注入
   借阅日期、归还日期、逾期计算依赖当前时间函数，测试边界日期时不够灵活。

4. 输入层与业务层的数据边界不够明确
   一些字段转换如字符串转整数分散在不同层，容易造成测试样例覆盖不全。

5. 异常路径覆盖不足
   当前明显存在很多边界条件，但没有对应测试验证，例如：
   - 用户不存在
   - 图书不存在
   - 用户状态异常
   - 库存不足
   - 已重复借阅
   - 借阅上限达到 10 本
   - 归还不存在的记录
   - 重复归还

### 5.2 实验二最推荐的改进动作

建议你做下面这些，能很好满足指导书要求：

1. 为 `LibraryService` 编写单元测试
   重点覆盖：
   - `add_book()`
   - `update_book()`
   - `add_user()`
   - `borrow_book()`
   - `return_book()`
   - `get_overdue_records()`

2. 引入测试专用存储
   利用 `LibraryService(storage=...)` 的注入点，编写一个简单的 `FakeStorage` 或使用临时目录，避免测试污染真实 JSON 文件。

3. 解耦时间依赖
   如果工作量允许，可以把“当前日期获取”改为可注入函数，或者在测试中通过参数控制日期，这样边界测试更好写。

4. 增加边界测试和异常路径测试
   这是实验二里最关键的得分点，因为指导书专门强调 AI 代码往往在 happy path 上表现正常，但边界路径处理不足。

5. 统计覆盖率
   使用 `pytest + coverage.py` 或 `pytest-cov`，至少给出行覆盖率结果。

### 5.3 实验二建议优先写哪些测试

建议最少完成以下 8 组测试：

1. 正常添加图书成功
2. 重复 ISBN 添加失败
3. 正常添加用户成功
4. 重复手机号添加失败
5. 正常借书成功且库存减 1
6. 库存不足时借书失败
7. 同一用户重复借同一本未归还图书失败
8. 正常还书成功且库存恢复

如果时间允许，再补充：

9. 用户状态异常时借书失败
10. 借阅数量达到上限时失败
11. 归还已归还记录失败
12. 逾期记录计算正确

## 6. 两个实验的推荐实施顺序

为了让 Git 提交历史和实验叙事更自然，建议按下面顺序做：

### 第一步：建立实验前基线

- 保留当前版本作为初始版本。
- 运行 `Pylint`、`Radon`、`CodeQL`，记录重构前数据。
- 截图保存：
  - 静态分析结果
  - 复杂度或可维护性指标
  - 当前项目目录结构

### 第二步：完成实验一重构

- 先做结构性重构，不急着写测试。
- 重构后重新执行 `Pylint`、`Radon`、`CodeQL`。
- 对比前后指标。

建议 Git 提交示例：

1. `refactor: extract finder and validation helpers`
2. `refactor: split borrow and return workflows`
3. `refactor: simplify cli menu handlers and rename variables`

### 第三步：完成实验二可测试性改造

- 新建测试目录，例如 `tests/`
- 补充 `pytest` 测试
- 增加 `coverage` 统计
- 如有必要，小幅调整代码结构以方便测试

建议 Git 提交示例：

1. `test: add fake storage and service unit tests`
2. `test: cover borrow and return edge cases`
3. `chore: add coverage report and test instructions`

### 第四步：整理报告素材

- 收集 before/after 数据表
- 收集关键代码前后对比图
- 收集 Git 提交截图
- 收集 CodeQL 和覆盖率截图

## 7. 报告如何合并写

建议你写成“一份报告，两个实验项目”结构，既符合模板，也方便老师阅读。

## 8. 合并报告推荐目录

你可以直接按下面的目录写：

1. 相关知识
2. 实验对象与需求描述
3. 系统结构与初始问题分析
4. 实验一：面向可读性与可修改性的重构
5. 实验二：面向可测试性的改进与验证
6. 工具度量与结果对比
7. 关键代码说明
8. 结论
9. AI 使用声明
10. 附录

## 9. 各章节具体写法

### 9.1 相关知识

本章分两部分：

- 可读性、可修改性、可测试性的概念与重要性
- AI 生成代码常见问题

你可以这样写：

- 可读性关注代码是否易于理解，包括命名、结构、注释和控制流程清晰度。
- 可修改性关注系统在需求变化时能否低成本地完成局部修改而不引发连锁影响。
- 可测试性关注系统是否便于构造输入、观察输出、隔离依赖并覆盖边界路径。
- 在 AI 辅助开发背景下，代码往往“先可运行，再考虑可维护”，因此更容易出现职责混杂、边界条件不足、抽象过早或不当的问题。

这里还可以简单介绍 ICONIX 思想：从用例行为回到类与方法，检查每个模块是否服务于明确需求。

### 9.2 实验对象与需求描述

这部分建议写：

- 项目名称：命令行图书馆管理系统
- 技术栈：Python 3.11、JSON 本地持久化、命令行交互
- 主要功能：图书管理、用户管理、借还书、逾期检查、统计信息
- 生成背景：属于 AI 辅助生成后保留典型可重构问题的课程实验项目

非功能需求建议这样定义：

- 实验一目标：降低核心业务函数复杂度，提高命名与职责清晰度，提升代码可修改性。
- 实验二目标：建立基础自动化测试体系，为核心业务规则提供单元测试与覆盖率度量。

### 9.3 系统结构与初始问题分析

可以直接按当前结构描述：

- `main.py`：CLI 入口与菜单流程控制
- `services.py`：核心业务逻辑
- `models.py`：实体模型
- `storage.py`：JSON 持久化
- `utils.py`：输入输出与日期等工具函数

然后写初始问题：

- 表现层与业务层分离不彻底
- 业务函数职责过重
- 重复校验逻辑较多
- 测试代码缺失
- 时间与存储依赖影响可测试性

### 9.4 实验一：可读性与可修改性

这一章建议按“问题 -> 改进 -> 效果”来写。

可写的内容示例：

- 原始问题：`borrow_book()` 同时完成用户查找、图书查找、借阅资格判断、记录创建和持久化保存，后续新增规则会扩大修改范围。
- 改进策略：将查找逻辑、资格校验逻辑和保存逻辑拆分成更小的私有方法，并统一变量命名。
- 改进效果：单个函数逻辑更集中，新增规则时只需修改局部辅助函数，降低回归风险。

再列出 3 到 5 个改进点即可，不要把所有小改动都写进正文。

### 9.5 实验二：可测试性

这一章建议按“短板 -> 解耦 -> 测试 -> 覆盖率”来写。

可以这样组织：

- 原始状态：项目缺少自动化测试，业务规则只能通过命令行手工验证。
- 可测试性问题：业务逻辑与存储保存耦合、边界条件多但缺少系统验证。
- 改进措施：
  - 增加测试目录和测试用例
  - 使用假存储或临时目录隔离数据文件
  - 为借阅、归还、逾期等核心流程编写边界测试
- 结果分析：覆盖率提升，关键路径具备自动回归能力，后续重构更安全。

### 9.6 工具度量与结果对比

建议做成表格。

实验一可比较：

| 指标 | 改进前 | 改进后 | 变化 |
| --- | --- | --- | --- |
| `borrow_book()` 圈复杂度 | 待测 | 待测 | 下降 |
| `return_book()` 圈复杂度 | 待测 | 待测 | 下降 |
| 平均复杂度 | 待测 | 待测 | 下降 |
| 可维护性指数 | 待测 | 待测 | 上升 |
| Pylint 评分 | 待测 | 待测 | 上升 |

实验二可比较：

| 指标 | 改进前 | 改进后 | 变化 |
| --- | --- | --- | --- |
| 自动化测试数量 | 0 | 待测 | 上升 |
| 行覆盖率 | 0% 或未统计 | 待测 | 上升 |
| 边界测试数量 | 0 | 待测 | 上升 |

### 9.7 关键代码说明

建议挑 3 类代码：

1. 重构前后的长函数对比
2. 提取后的公共校验函数
3. 新增的测试用例

代码不宜贴太长，每段 20 到 40 行左右即可，并在文字里说明它与哪项质量属性相关。

### 9.8 结论

结论建议分三点：

1. 本项目通过人工重构弥补了 AI 初始代码中“能运行但难维护、难验证”的问题。
2. 可读性/可修改性提升后，实验二中的测试补充工作明显更顺利，说明质量属性之间存在相互促进关系。
3. AI 可以帮助生成初稿和测试样例，但质量判断、边界识别、结构权衡仍然依赖工程师完成。

### 9.9 AI 使用声明

这是必须写的。你可以按实际情况修改后直接使用：

> 本实验使用了 AI 工具辅助完成部分工作，包括代码初稿生成、重构思路讨论、实验报告提纲整理等。所有 AI 生成内容均经过人工审查、修改和验证。对于关键业务逻辑、实验数据分析、结论归纳与最终报告内容，均由本人独立核查并负责。

## 10. 截图与附件清单

最终提交前，建议准备以下截图或附件：

- 初始项目结构图或目录截图
- 重构前 `Pylint` / `Radon` / `CodeQL` 结果截图
- 重构后 `Pylint` / `Radon` / `CodeQL` 结果截图
- 测试运行成功截图
- 覆盖率结果截图
- Git 提交历史截图
- 关键代码前后对比截图

## 11. 你当前项目最值得优先改的代码位置

如果你马上要动手做实验，我建议优先处理下面这些位置：

- `services.py`
  - `borrow_book()`
  - `return_book()`
  - `update_book()`
  - `update_user()`
  - 重复查找与重复校验逻辑

- `main.py`
  - `book_menu()`
  - `user_menu()`
  - `borrow_menu()`

- `utils.py`
  - 输入与展示函数可先不大改，只做必要整理

- `storage.py`
  - 为实验二服务，可以考虑增加更容易测试的替代实现或测试夹具

## 12. 一个可执行的最小完成方案

如果你时间不多，按下面这个“最小可交付方案”做，依然能形成完整实验闭环：

### 实验一最小方案

- 重构 `borrow_book()`
- 重构 `return_book()`
- 提取公共查找函数
- 统一主要变量命名
- 跑一次 `Pylint`、`Radon`、`CodeQL`

### 实验二最小方案

- 新建 `tests/test_services.py`
- 补充 8 个核心单元测试
- 跑一次覆盖率统计

### 报告最小方案

- 合并写成一份报告
- 保证有：
  - 项目介绍
  - 问题分析
  - 重构过程
  - 测试过程
  - 度量结果
  - Git 提交截图
  - AI 使用声明

## 13. 最后建议

这份项目最适合的策略不是“做很多炫技改造”，而是“围绕两个质量属性做有限但扎实的改进”：

- 实验一把重点放在**结构重构和可修改性提升**
- 实验二把重点放在**可测试性改造和测试覆盖**

这样两次实验之间逻辑连贯，报告也会很好写：

- 先说明为什么原始 AI 代码难改
- 再说明重构后为什么更容易写测试
- 最后用静态分析指标和覆盖率结果完成闭环

## 版本记录

- v1：基于实验模板、实验指导书和当前图书馆管理系统整理实验一、实验二实施与报告撰写指南。
